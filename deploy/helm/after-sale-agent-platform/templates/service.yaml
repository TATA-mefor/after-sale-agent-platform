apiVersion: v1
kind: Service
metadata:
  name: {{ include "after-sale-agent-platform.fullname" . }}
  labels:
    {{- include "after-sale-agent-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "after-sale-agent-platform.selectorLabels" . | nindent 4 }}
