apiVersion: v1
kind: Service
metadata:
  name: after-sale-agent-platform
  labels:
    app: after-sale-agent-platform
spec:
  type: ClusterIP
  selector:
    app: after-sale-agent-platform
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
